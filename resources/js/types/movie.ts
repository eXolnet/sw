export interface Movie {
    id?: number
    title: string
    episode_id: number | null
    opening_crawl: string
    director: string
    producer: string
    release_date: string
    created: string
    edited: string
}
